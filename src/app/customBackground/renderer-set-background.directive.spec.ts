import { RendererSetBackgroundDirective } from './renderer-set-background.directive';

describe('RendererSetBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new RendererSetBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
