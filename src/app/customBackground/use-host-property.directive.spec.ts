import { UseHostPropertyDirective } from './use-host-property.directive';

describe('UseHostPropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new UseHostPropertyDirective();
    expect(directive).toBeTruthy();
  });
});
